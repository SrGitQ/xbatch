const Logo = ({name}) => {
    return (
        <div className="bg-bray rounded-full w-28 h-28 mx-auto flex">
            <p className="text-grayrise text-center mx-auto font-bold m-auto">{name}</p>
        </div>
    )
}

export default Logo;